---

- name: install base homebrew packages
  homebrew:
    name: "{{item}}"
    state: present
  with_items:
    - ansible
    - git
    - npm
    - openssl
    - rbenv
    - rbenv-gem-rehash
    - rust
    - tmux
    - tree

- name: install gems
  gem:
    name: "{{item}}"
    state: present
  with_items:
    - jekyll

- name: install npm packages
  npm:
    name: "{{item}}"
    state: present
    global: yes
  with_items:
    - keybase-installer

